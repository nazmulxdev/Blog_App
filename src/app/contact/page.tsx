import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description: "This is the contact page of this project.",
};

export default function ContactPage() {
  return <div>This is contact page.</div>;
}
